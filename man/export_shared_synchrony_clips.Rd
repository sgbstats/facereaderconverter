% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_shared_synchrony_clips.R
\name{export_shared_synchrony_clips}
\alias{export_shared_synchrony_clips}
\title{Export clips for shared synchronous episodes}
\usage{
export_shared_synchrony_clips(
  coded_data,
  shared_synchrony,
  video_paths,
  n = 10L,
  emotion = "happy",
  optimised_subject = "both",
  only_synchronies = TRUE,
  buffer = 0,
  buffer_units = c("seconds", "frames"),
  buffer_frames = 0L,
  buffer_seconds = 0,
  output_path = NULL,
  output = c("zip", "folder"),
  overwrite = FALSE,
  ffmpeg = "ffmpeg"
)
}
\arguments{
\item{coded_data}{A converted \code{fr_coding} object with \code{metadata$fps}.}

\item{shared_synchrony}{A table returned by \code{\link[=shared_synchronous_episodes]{shared_synchronous_episodes()}}.}

\item{video_paths}{Named character vector of source video paths. Names must
match values in the shared-synchrony \code{id} column.}

\item{n}{Number of highest-ranked intervals to export. Use \code{0} to export all
intervals remaining after filtering.}

\item{emotion}{Optional character vector of emotions to retain.}

\item{optimised_subject}{Subject-name regular expression used to rank clips by
one subject's maximum emotion value. Defaults to \code{"both"}, which ranks shared
synchronies by their combined value.}

\item{only_synchronies}{Logical; when \code{TRUE}, select shared synchronies. When
\code{FALSE}, select individual episodes for \code{optimised_subject}. Ignored when
\code{optimised_subject = "both"}.}

\item{buffer}{A non-negative number for a symmetric buffer, or a named
two-element vector with names \code{before} and \code{after} for asymmetric buffers.
Units are controlled by \code{buffer_units}.}

\item{buffer_units}{Buffer units: \code{"seconds"} or \code{"frames"}.}

\item{buffer_frames}{Non-negative whole-number symmetric frame buffer. Kept
for compatibility; use \code{buffer} and \code{buffer_units} for new code.}

\item{buffer_seconds}{Non-negative symmetric time buffer in seconds. Kept
for compatibility; use \code{buffer} and \code{buffer_units} for new code.}

\item{output_path}{Optional ZIP file path when \code{output = "zip"}, or output
directory when \code{output = "folder"}. By default, output is placed beside the
selected source video. All selected videos must then be in the same folder.}

\item{output}{Output type: \code{"zip"} (the default) or \code{"folder"}.}

\item{overwrite}{Logical; overwrite an existing ZIP archive or non-empty
output folder. Defaults to \code{FALSE}.}

\item{ffmpeg}{Path to the FFmpeg executable or its command name.}
}
\value{
A \code{data.table} manifest invisibly. For ZIP output, the manifest is
also included in the archive as \code{manifest.csv}.
}
\description{
Selects shared synchronous intervals with the largest combined emotion values,
or a named subject's highest-valued shared or individual episodes, and exports
the corresponding video segments with FFmpeg. Frame ranges are inclusive: a
range from frame 0 through frame 0 has duration \code{1 / fps}.
}
\examples{
\dontrun{
coded_data <- convert_to_episodes(coding_data)
shared <- shared_synchronous_episodes(coded_data)
export_shared_synchrony_clips(
  coded_data,
  shared,
  video_paths = c("1" = "recording.mp4"),
  n = 10,
  optimised_subject = "child",
  only_synchronies = FALSE,
  buffer = c(before = 2, after = 1),
  output_path = "recording-shared-clips.zip"
)
}
}
