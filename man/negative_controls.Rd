% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/negative_controls.R
\name{negative_controls}
\alias{negative_controls}
\title{Calculate synchrony for matched negative-control intervals}
\usage{
negative_controls(
  coded_data,
  episodes,
  mutually_exclusive = FALSE,
  subject = "subject",
  id = "id",
  time_limit = 3,
  time_limit_frames = NULL,
  constraint_method = "episode",
  fps = 30L,
  missing_threshold = 0,
  exclude_emotions = "neutral",
  max_tries = 1000L
)
}
\arguments{
\item{coded_data}{Output from \code{\link[=convert_to_episodes]{convert_to_episodes()}}.}

\item{episodes}{A data frame containing episode ranges. It must contain the
selected ID column, either the selected subject column or \code{denominator}, and
\code{emotion}, \code{run_id}, \code{start_frame}, and \code{end_frame}. Frame bounds are
inclusive.}

\item{mutually_exclusive}{Logical scalar. When \code{TRUE}, controls cannot overlap
any supplied episode for the same ID. Defaults to \code{FALSE}.}

\item{subject}{Character scalar giving the subject column. Defaults to
\code{"subject"}.}

\item{id}{Character scalar giving the ID column. Defaults to \code{"id"}.}

\item{time_limit}{Maximum episode-window length in seconds. \code{NULL} is
allowed when \code{time_limit_frames} is supplied or when \code{constraint_method}
is \code{"episode"}. Default is \code{3}.}

\item{time_limit_frames}{Optional maximum episode-window length in frames.
If supplied, this takes precedence over \code{time_limit}.}

\item{constraint_method}{Character scalar controlling how the episode window
ends: \code{"strict"}, \code{"episode"}, \code{"loose"}, or \code{"frames"}. Default is
\code{"episode"}.}

\item{fps}{Frames per second used to convert \code{time_limit} to frames.
Default is \code{30}.}

\item{missing_threshold}{Numeric scalar in \verb{[0, 1]}. Denominator and numerator episodes are
kept only when the comparison subject is present for at least this
proportion of frames within the episode. Default is \code{0}.}

\item{exclude_emotions}{Character vector of emotions to exclude from the
denominator calculation. Default is \code{"neutral"}.}

\item{max_tries}{Maximum number of random control starts attempted for each
episode. Defaults to \code{1000}.}
}
\value{
A data.table containing the usual \code{\link[=synchrony_by_episode]{synchrony_by_episode()}} columns
for matched controls, plus source and control identifiers, frame bounds,
attempt count, and matching status. Unmatched source episodes have missing
synchrony fields and \code{control_status = "unmatched"}.
}
\description{
Samples control intervals with the same inclusive frame length as supplied
episodes, then calculates synchrony for the sampled intervals. The \code{episodes}
argument can be the output of \code{\link[=synchrony_by_episode]{synchrony_by_episode()}} or a compatible episode
table. By default, controls may overlap known episodes.
}
\details{
When \code{coded_data} contains metadata, its \code{metadata$fps} value is
used by the synchrony calculations, including when \code{fps} is supplied.
}
\examples{
\dontrun{
coded_data <- convert_to_episodes(coding_data)
episodes <- synchrony_by_episode(coded_data)
negative_controls(coded_data, episodes)
}
}
