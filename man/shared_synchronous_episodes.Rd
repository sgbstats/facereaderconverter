% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synchrony.R
\name{shared_synchronous_episodes}
\alias{shared_synchronous_episodes}
\title{Map shared synchronous episodes}
\usage{
shared_synchronous_episodes(
  coded_data,
  episodes = NULL,
  subject = "subject",
  id = "id",
  time_limit = 3,
  time_limit_frames = NULL,
  constraint_method = "episode",
  fps = 30L,
  missing_threshold = 0,
  exclude_emotions = "neutral"
)
}
\arguments{
\item{coded_data}{Output from \code{convert_to_episodes()}.}

\item{episodes}{Optional replacement episode data frame. It must contain the
selected ID and subject columns plus \code{emotion}, \code{run_id}, \code{start_frame}, and
\code{end_frame}. Frame bounds are inclusive. A \code{synchrony_by_episode()} result
is also accepted, using its \code{denominator} column as the episode subject.}

\item{subject}{Character scalar giving the column in \code{coded_data$coding} and
\code{coded_data$episodes} that identifies the subject. Default is \code{"subject"}.}

\item{id}{Character scalar giving the column in \code{coded_data$coding} and
\code{coded_data$episodes} that identifies the case or dyad. Default is \code{"id"}.}

\item{time_limit}{Maximum episode-window length in seconds. \code{NULL} is
allowed when \code{time_limit_frames} is supplied or when \code{constraint_method}
is \code{"episode"}. Default is \code{3}.}

\item{time_limit_frames}{Optional maximum episode-window length in frames.
If supplied, this takes precedence over \code{time_limit}.}

\item{constraint_method}{Character scalar controlling how the episode window
ends: \code{"strict"}, \code{"episode"}, \code{"loose"}, or \code{"frames"}. Default is
\code{"episode"}.}

\item{fps}{Frames per second used to convert \code{time_limit} to frames.
Default is \code{30}.}

\item{missing_threshold}{Numeric scalar in \verb{[0, 1]}. Denominator and numerator episodes are
kept only when the comparison subject is present for at least this
proportion of frames within the episode. Default is \code{0}.}

\item{exclude_emotions}{Character vector of emotions to exclude from the
denominator calculation. Default is \code{"neutral"}.}
}
\value{
A data.table with episode-pair identifiers, their inclusive shared
frame interval, each source episode's maximum value, the larger maximum,
and their combined value.
}
\description{
Maps pairs of same-emotion episodes that overlap while both subjects are in
state. Each unordered pair of source episodes is returned once. A source
episode may therefore occur in multiple rows when it overlaps more than one
episode of the other subject.
}
\examples{
\dontrun{
coded_data <- convert_to_episodes(coding_data)
shared_synchronous_episodes(coded_data)
}
}
\seealso{
\code{\link[=synchrony_by_episode]{synchrony_by_episode()}}
}
