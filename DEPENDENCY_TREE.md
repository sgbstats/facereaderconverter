# R Function Dependency Tree

_Generated from static source inspection; direct dependencies only._

| Function | Depends on | Depended on by | Required structure and inputs |
|---|---|---|---|
| `add_delta_column()` | `all_deltas()`<br>`parse_time_to_frame()` | `delta()` | Signature: `add_delta_column(coding, delta = 0.1, delta_window = 0.2, fps = 30L, cores = 0L)`; `coding`: data frame or `fr_coding`; expected `id`, `subject`, `emotion`, `value`, and `frame` or `video_time`; `delta`, `delta_window`: positive numeric scalars (the implementation initially permits `delta_window = 0` but later uses a positive-value check); `fps`: positive integer scalar; `cores`: numeric thread count |
| `convertFRDirectory()` | `convertFRFiles()`<br>`map_paths()` | — | Signature: `convertFRDirectory(inpath, outpath = inpath, recursive = TRUE, pattern = NULL, values_as_numeric = TRUE, clean_names = TRUE, save_metadata = outpath, metadata_filename = "metadata.csv", fail_codes = FALSE, duplicate_timecodes_as_error = TRUE, cores = 0L, ...)`; `inpath`: input directory; `outpath`: output directory; optional file-selection and conversion controls; enumerates `.txt` files and optionally writes metadata CSV |
| `convertFRExcelFiles()` | `check_duplicate_timecodes()`<br>`detect_fr_header_row()` | `loadFRfile()` | Signature: `convertFRExcelFiles(inpath, return_data = TRUE, values_as_numeric = TRUE, clean_names = TRUE, fail_codes = FALSE, duplicate_timecodes_as_error = TRUE, sheet = 1, ...)`; `inpath`: existing `.xlsx` file; `sheet`: positive in-range sheet index; FaceReader header row and, for metadata, expected rows 5–6; optional numeric conversion, name cleaning, and failure-code controls |
| `convertFRFiles()` | `check_duplicate_timecodes()`<br>`detect_fr_header_row()` | `convertFRDirectory()`<br>`loadFRfile()` | Signature: `convertFRFiles(inpath, outpath = paste0(tools::file_path_sans_ext(inpath), ".csv"), return_data = FALSE, values_as_numeric = TRUE, clean_names = TRUE, fail_codes = FALSE, duplicate_timecodes_as_error = TRUE, ...)`; `inpath`: existing `.txt` file; `outpath`: output path; FaceReader metadata and detectable `Video Time` header; optional delimiter-compatible data and detailed-export columns; optional numeric conversion, name cleaning, and failure-code controls |
| `convert_to_episodes()` | `all_deltas()`<br>`hysteresis_state()`<br>`parse_time_to_frame()` | — | Signature: `convert_to_episodes(coding_df, T_up = 0.20, T_down = 0.1, delta = 0.10, delta_window = 0.2, min_dur_sec = 0.1, consecutive_missing = 150L, fps = 30L, cores = 0L)`; `coding_df`: data frame with `id`, `subject`, and `frame` or `video_time`; either long `emotion`/`value` columns or wide values; `T_up`, `T_down`: numeric scalars in $[0,1]$ with `T_up >= T_down`; positive `delta`, `delta_window`, `min_dur_sec`; non-negative integer `consecutive_missing`; positive integer `fps`; `cores`: numeric thread count |
| `delta()` | `add_delta_column()` | — | Signature: `delta(coding, delta = 0.1, delta_window = 0.2, fps = 30L, cores = 0L)`; same input structure and scalar controls as `add_delta_column()`; deprecated alias |
| `delta_episodes()` | — | — | Signature: `delta_episodes(coding, fps = 30L, cores = 0L)`; `coding`: table-like object with `delta`, `id`, `subject`, `emotion`, `frame`, `video_time`, and `value` columns; `fps`: frames-per-second value; `cores`: numeric thread count |
| `loadFRfile()` | `convertFRExcelFiles()`<br>`convertFRFiles()` | — | Signature: `loadFRfile(inpath, values_as_numeric = TRUE, clean_names = TRUE, fail_codes = FALSE, duplicate_timecodes_as_error = TRUE, sheet = 1, ...)`; `inpath`: existing `.txt`, `.xlsx`, or `.csv` file; `.xlsx` uses `sheet`; `.txt`/`.xlsx` require FaceReader-compatible input; optional conversion and cleaning controls |
| `locf()` | — | — | Signature: `locf(coding, fps = 30, consecutive_missing = NULL)`; `coding`: table-like object or `fr_coding` with `id`, `subject`, `emotion`, `video_time`, `value`, `run_id`, and `in_state`; `fps`: frames-per-second value; optional `consecutive_missing`; `fr_coding` input also supplies metadata and deltas |
| `map_paths()` | — | `convertFRDirectory()` | Signature: `map_paths(input_dir, output_dir, files)`; `input_dir`, `output_dir`: character scalar roots; `files`: character paths under `input_dir`; creates missing destination directories |
| `parse_time_to_frame()` | — | `add_delta_column()`<br>`convert_to_episodes()` | Signature: `parse_time_to_frame(video_time, fps)`; `video_time`: character or numeric values in `HH:MM:SS`, `MM:SS`, or seconds format; `fps`: numeric frames-per-second value |
| `reaction_rate()` | `build_reaction_rate_episode_table()`<br>`prepare_reaction_rate_inputs()` | — | Signature: `reaction_rate(coded_data, time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", exclude_start = 0.1, exclude_start_frames = NULL, fps = 30L, subject_names = NULL, exclude_emotions = "neutral", minimum_threshold = 0)`; `coded_data`: `fr_coding`-like list with `coding`, `episodes`, and `deltas`, or a coding table; list input requires expected episode/delta fields; positive `time_limit` and `fps`; valid frame limits; `constraint_method`: one of `strict`, `episode`, `loose`, `frames`; `minimum_threshold`: numeric scalar in $[0,1]$; optional subject/emotion filters |
| `reaction_rate_by_episode()` | `build_reaction_rate_episode_table()`<br>`prepare_reaction_rate_inputs()` | — | Signature: `reaction_rate_by_episode(coded_data, time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", exclude_start = 0.1, exclude_start_frames = NULL, fps = 30L, subject_names = NULL, exclude_emotions = "neutral", minimum_threshold = 0)`; same input structure and validation controls as `reaction_rate()`; returns episode-level results |
| `synchrony()` | `build_synchrony_episode_table()`<br>`prepare_synchrony_inputs()` | — | Signature: `synchrony(coded_data, subject = "subject", id = "id", time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", fps = 30L, missing_threshold = 0, exclude_emotions = "neutral")`; `coded_data`: list with `coding` and `episodes`; coding requires selected `id`/`subject` columns plus `emotion`, `video_time`, `value`, `in_state`, `run_id` and optionally `frame`; episodes require selected `id`/`subject` plus `emotion`, `run_id`, `start_frame`, `end_frame`; constrained methods require a time or frame limit; optional emotion exclusions |
| `synchrony_by_episode()` | `build_synchrony_episode_table()`<br>`prepare_synchrony_inputs()` | — | Signature: `synchrony_by_episode(coded_data, subject = "subject", id = "id", time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", fps = 30L, missing_threshold = 0, exclude_emotions = "neutral")`; same input structure and validation controls as `synchrony()`; returns episode-level results |
| `to_seconds()` | — | — | Signature: `to_seconds(x, digits = 3L)`; `x`: character vector matching `hh:mm:ss` with optional fractional seconds; `digits`: non-negative numeric scalar controlling retained fractional digits |

## Notes

- Export status is authoritative from `NAMESPACE`; the table contains its 17 exported functions. The internal functions `all_deltas()`, `hysteresis_state()`, `build_reaction_rate_episode_table()`, `prepare_reaction_rate_inputs()`, `build_synchrony_episode_table()`, `prepare_synchrony_inputs()`, `check_duplicate_timecodes()`, and `detect_fr_header_row()` are included only as dependency targets.
- Native wrappers `all_deltas()` and `hysteresis_state()` are defined in `R/RcppExports.R` but require the compiled package shared library at runtime. Their native implementations were not inspected because they are outside the R source inspected here.
- `convertFRDirectory()` calls the `parallel` namespace, although `parallel` is not declared in `DESCRIPTION`; this is an undeclared package dependency rather than a project-function dependency.
- `delta_episodes()` calls unqualified `setDT()`, relying on the package-level `data.table` import.
- Calls through dynamic dispatch, non-standard evaluation, strings, and runtime-generated objects were not treated as definite dependencies. No duplicate top-level function definitions were found.
