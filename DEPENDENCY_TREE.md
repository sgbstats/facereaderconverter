# R Function Dependency Tree

_Generated from static source inspection; direct dependencies only._

| Function | Depends on | Depended on by | Required structure and inputs |
|---|---|---|---|
| `add_delta_column()` | `all_deltas()`<br>`parse_time_to_frame()` | `delta()` | Signature: `add_delta_column(coding, delta = 0.1, delta_window = 0.2, fps = 30L, cores = 0L)`; `coding`: data frame or `fr_coding`; expected `id`, `subject`, `emotion`, `value`, and `frame` or `video_time`; positive numeric `delta`; non-negative numeric `delta_window`; positive whole-number `fps`; numeric `cores` |
| `convertFRDirectory()` | `convertFRFiles()`<br>`map_paths()` | — | Signature: `convertFRDirectory(inpath, outpath = inpath, recursive = TRUE, pattern = NULL, values_as_numeric = TRUE, clean_names = TRUE, save_metadata = outpath, metadata_filename = "metadata.csv", fail_codes = FALSE, duplicate_timecodes_as_error = TRUE, cores = 0L, ...)`; input/output directory paths; optional file-selection, conversion, metadata, and parallel-processing controls |
| `convertFRExcelFiles()` | `check_duplicate_timecodes()`<br>`detect_fr_header_row()` | `loadFRfile()` | Signature: `convertFRExcelFiles(inpath, return_data = TRUE, values_as_numeric = TRUE, clean_names = TRUE, fail_codes = FALSE, duplicate_timecodes_as_error = TRUE, sheet = 1, ...)`; existing `.xlsx` path; positive in-range sheet index; FaceReader header row; optional numeric conversion, name cleaning, and failure-code controls |
| `convertFRFiles()` | `check_duplicate_timecodes()`<br>`detect_fr_header_row()` | `convertFRDirectory()`<br>`loadFRfile()` | Signature: `convertFRFiles(inpath, outpath = paste0(tools::file_path_sans_ext(inpath), ".csv"), return_data = FALSE, values_as_numeric = TRUE, clean_names = TRUE, fail_codes = FALSE, duplicate_timecodes_as_error = TRUE, ...)`; existing `.txt` path; FaceReader metadata and detectable `Video Time` header; optional output and conversion controls |
| `convert_to_episodes()` | `all_deltas()`<br>`hysteresis_state()`<br>`parse_time_to_frame()` | — | Signature: `convert_to_episodes(coding_df, T_up = 0.20, T_down = 0.1, delta = 0.10, delta_window = 0.2, min_dur_sec = 0.1, consecutive_missing = 150L, fps = 30L, cores = 0L)`; data frame with `id`, `subject`, and `frame` or `video_time`; long `emotion`/`value` columns or wide values; thresholds in `[0,1]`; positive duration/delta controls; non-negative missing-frame limit; positive whole-number `fps` |
| `delta()` | `add_delta_column()` | — | Signature: `delta(coding, delta = 0.1, delta_window = 0.2, fps = 30L, cores = 0L)`; same coding structure and scalar controls as `add_delta_column()`; deprecated alias |
| `delta_episodes()` | — | — | Signature: `delta_episodes(coding, fps = 30L, cores = 0L)`; table-like object with `delta`, `id`, `subject`, `emotion`, `frame`, `video_time`, and `value` columns; numeric thread count |
| `loadFRfile()` | `convertFRExcelFiles()`<br>`convertFRFiles()` | — | Signature: `loadFRfile(inpath, values_as_numeric = TRUE, clean_names = TRUE, fail_codes = FALSE, duplicate_timecodes_as_error = TRUE, sheet = 1, ...)`; existing `.txt`, `.xlsx`, or `.csv` path; dispatches by extension; optional import and cleaning controls |
| `locf()` | — | — | Signature: `locf(coding, fps = 30, consecutive_missing = NULL)`; table-like object or `fr_coding` with `id`, `subject`, `emotion`, `video_time`, `value`, `run_id`, and `in_state`; optional missing-frame limit |
| `map_paths()` | — | `convertFRDirectory()` | Signature: `map_paths(input_dir, output_dir, files)`; character input/output roots and file paths under `input_dir`; creates destination directories |
| `parse_time_to_frame()` | — | `add_delta_column()`<br>`convert_to_episodes()`<br>`prepare_synchrony_inputs()` | Signature: `parse_time_to_frame(video_time, fps)`; character or numeric `HH:MM:SS`, `MM:SS`, or seconds values; numeric frames-per-second |
| `reaction_rate()` | `build_reaction_rate_episode_table()`<br>`prepare_reaction_rate_inputs()` | — | Signature: `reaction_rate(coded_data, time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", exclude_start = 0.1, exclude_start_frames = NULL, fps = 30L, subject_names = NULL, exclude_emotions = "neutral", minimum_threshold = 0)`; `fr_coding`-like list with `coding`, `episodes`, and `deltas`, or a validated coding table; valid time/frame limits, subject/emotion filters, and threshold controls |
| `reaction_rate_by_episode()` | `build_reaction_rate_episode_table()`<br>`prepare_reaction_rate_inputs()` | — | Signature: `reaction_rate_by_episode(coded_data, time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", exclude_start = 0.1, exclude_start_frames = NULL, fps = 30L, subject_names = NULL, exclude_emotions = "neutral", minimum_threshold = 0)`; same input structure and validation controls as `reaction_rate()`; returns episode-level results |
| `synchrony()` | `build_synchrony_episode_table()`<br>`prepare_synchrony_inputs()` | — | Signature: `synchrony(coded_data, subject = "subject", id = "id", time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", fps = 30L, missing_threshold = 0, exclude_emotions = "neutral")`; list with `coding` and `episodes`; coding requires selected id/subject plus `emotion`, `video_time`, `value`, `in_state`, and `run_id`; episode table requires selected id/subject plus `emotion`, `run_id`, `start_frame`, and `end_frame` |
| `synchrony_by_episode()` | `build_synchrony_episode_table()`<br>`prepare_synchrony_inputs()` | — | Signature: `synchrony_by_episode(coded_data, subject = "subject", id = "id", time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", fps = 30L, missing_threshold = 0, exclude_emotions = "neutral")`; same input structure and validation controls as `synchrony()`; returns episode-level results |
| `to_seconds()` | — | — | Signature: `to_seconds(x, digits = 3L)`; character vector matching `hh:mm:ss` with optional fractional seconds; non-negative numeric fractional-digit control |

## Internal and helper functions

| Function | Depends on | Depended on by | Required structure and inputs |
|---|---|---|---|
| `all_deltas()` | — | `add_delta_column()`<br>`convert_to_episodes()` | Signature: `all_deltas(value, k, delta)`; native wrapper accepting a value vector, integer lag/window, and numeric delta threshold |
| `build_reaction_rate_episode_table()` | — | `reaction_rate()`<br>`reaction_rate_by_episode()` | Signature: `build_reaction_rate_episode_table(inputs)`; validated list from `prepare_reaction_rate_inputs()` containing coding, episodes, deltas, limits, thresholds, and empty result templates |
| `build_synchrony_episode_table()` | — | `synchrony()`<br>`synchrony_by_episode()` | Signature: `build_synchrony_episode_table(inputs)`; validated list from `prepare_synchrony_inputs()` containing coding, episodes, limits, thresholds, and an empty result template |
| `check_duplicate_timecodes()` | — | `convertFRExcelFiles()`<br>`convertFRFiles()` | Signature: `check_duplicate_timecodes(df, duplicate_timecodes_as_error = TRUE)`; data frame with `Video Time`, optionally `Participant Name` and `Analysis Index`; error or warning policy for duplicates |
| `detect_fr_header_row()` | — | `convertFRExcelFiles()`<br>`convertFRFiles()` | Signature: `detect_fr_header_row(lines)`; character lines containing a `Video Time` header; returns the first matching row index |
| `hysteresis_state()` | — | `convert_to_episodes()` | Signature: `hysteresis_state(v, k, T_up, T_down, delta, min_len, consecutive_missing)`; native wrapper accepting value vector and episode threshold/window controls |
| `is_reaction_rate_scalar()` | — | `is_reaction_rate_whole()`<br>`prepare_reaction_rate_inputs()` | Signature: `is_reaction_rate_scalar(x)`; scalar value that is not `NA` |
| `is_reaction_rate_whole()` | `is_reaction_rate_scalar()` | `prepare_reaction_rate_inputs()` | Signature: `is_reaction_rate_whole(x)`; numeric scalar satisfying the package whole-number tolerance |
| `prepare_reaction_rate_inputs()` | `is_reaction_rate_scalar()`<br>`is_reaction_rate_whole()` | `reaction_rate()`<br>`reaction_rate_by_episode()` | Signature: `prepare_reaction_rate_inputs(coded_data, time_limit = 3, time_limit_frames = NULL, exclude_start = 0.1, exclude_start_frames = NULL, fps = 30L, subject_names = NULL, exclude_emotions = "neutral", minimum_threshold = 0, constraint_method = "episode")`; `fr_coding`-like list or coding table with required coding/episode/delta fields and validated window/filter controls |
| `prepare_synchrony_inputs()` | `parse_time_to_frame()` | `synchrony()`<br>`synchrony_by_episode()` | Signature: `prepare_synchrony_inputs(coded_data, subject = "subject", id = "id", time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", fps = 30L, missing_threshold = 0, exclude_emotions = "neutral")`; `fr_coding`-like list with coding and episodes; selected id/subject columns; required emotion, time/value/state/run fields; validated constraints |

## Notes

- Export status is authoritative from `NAMESPACE`: 17 exported functions are listed in the first table and 10 internal/helper functions in the second table.
- Both tables use the complete project-function graph. Dependency cells include internal/helper functions as well as exported functions; base R and third-party package calls are excluded.
- The two native wrappers `all_deltas()` and `hysteresis_state()` are defined in `R/RcppExports.R`; their native implementations were not inspected because they are outside the R source inspected here.
- Functions defined locally inside exported functions, such as `is_scalar`, `is_whole`, `process_file`, and path-manipulation closures, are not top-level project functions and are therefore not included as rows. Their calls are not represented as project-function dependencies.
- `convertFRDirectory()` calls the `parallel` namespace, although `parallel` is not declared in `DESCRIPTION`; this is an undeclared package dependency rather than a project-function dependency.
- `delta_episodes()` calls unqualified `setDT()`, relying on the package-level `data.table` import.
- Calls through dynamic dispatch, non-standard evaluation, strings, native `.Call()` implementations, and runtime-generated objects were not treated as definite dependencies. No duplicate top-level function definitions were found.
