# R Function Dependency Tree

_Generated from static source inspection; direct dependencies only._

| Function | Depends on | Depended on by | Required structure and inputs |
|---|---|---|---|
| `add_delta_column()` | `all_deltas()`<br>`parse_time_to_frame()` | `delta()` | Signature: `add_delta_column(coding, delta = 0.1, delta_window = 0.2, fps = 30L, cores = 0L)`; table-like `coding` with `id`, `subject`, `emotion`, and `frame` or `video_time` plus numeric values; scalar delta/window/fps/core controls |
| `convertFRDirectory()` | `convertFRFiles()`<br>`map_paths()` | — | Signature: `convertFRDirectory(inpath, outpath = inpath, recursive = TRUE, pattern = NULL, values_as_numeric = TRUE, clean_names = TRUE, save_metadata = outpath, metadata_filename = "metadata.csv", fail_codes = FALSE, duplicate_timecodes_as_error = TRUE, cores = 0L, ...)`; input/output directory paths and file-selection, conversion, metadata, and parallel-processing controls |
| `convertFRExcelFiles()` | `check_duplicate_timecodes()`<br>`detect_fr_header_row()` | `loadFRfile()` | Signature: `convertFRExcelFiles(inpath, return_data = TRUE, values_as_numeric = TRUE, clean_names = TRUE, fail_codes = FALSE, duplicate_timecodes_as_error = TRUE, sheet = 1, ...)`; existing Excel path, sheet selector, FaceReader header, and import/cleaning controls |
| `convertFRFiles()` | `check_duplicate_timecodes()`<br>`detect_fr_header_row()` | `convertFRDirectory()`<br>`loadFRfile()` | Signature: `convertFRFiles(inpath, outpath = paste0(tools::file_path_sans_ext(inpath), ".csv"), return_data = FALSE, values_as_numeric = TRUE, clean_names = TRUE, fail_codes = FALSE, duplicate_timecodes_as_error = TRUE, ...)`; existing FaceReader text path, output path, metadata/header, and conversion controls |
| `convert_to_episodes()` | `all_deltas()`<br>`hysteresis_state()`<br>`max_delta_ranges()`<br>`parse_time_to_frame()` | — | Signature: `convert_to_episodes(coding_df, T_up = 0.20, T_down = 0.1, delta = 0.10, delta_window = 0.2, min_dur_sec = 0.1, consecutive_missing = 150L, fps = 30L, cores = 0L)`; data frame with `id`, `subject`, and `frame` or `video_time`, plus long `emotion`/`value` or supported wide value columns; numeric threshold and timing controls |
| `delta()` | `add_delta_column()` | — | Signature: `delta(coding, delta = 0.1, delta_window = 0.2, fps = 30L, cores = 0L)`; same coding structure and scalar controls as `add_delta_column()`; deprecated wrapper |
| `delta_episodes()` | — | — | Signature: `delta_episodes(coding, fps = 30L, cores = 0L)`; table-like coding with `delta`, `id`, `subject`, `emotion`, `frame` or `video_time`, and `value` columns; numeric processing controls |
| `export_shared_synchrony_clips()` | `prepare_shared_synchrony_clips()` | — | Signature: `export_shared_synchrony_clips(coded_data, shared_synchrony, video_paths, n = 10L, emotion = "happy", buffer = 0, buffer_units = c("seconds", "frames"), buffer_frames = 0L, buffer_seconds = 0, output_path = NULL, output = c("zip", "folder"), overwrite = FALSE, ffmpeg = "ffmpeg")`; `fr_coding` object with `metadata$fps`, shared-synchrony table, named existing video paths, and FFmpeg/output controls |
| `loadFRfile()` | `convertFRExcelFiles()`<br>`convertFRFiles()` | — | Signature: `loadFRfile(inpath, values_as_numeric = TRUE, clean_names = TRUE, fail_codes = FALSE, duplicate_timecodes_as_error = TRUE, sheet = 1, ...)`; existing `.txt`, `.xlsx`, or `.csv` path with import and cleaning controls |
| `locf()` | — | — | Signature: `locf(coding, fps = 30, consecutive_missing = NULL)`; table-like coding with `id`, `subject`, `emotion`, `video_time`, `value`, `run_id`, and `in_state` columns; optional missing-frame limit |
| `map_paths()` | — | `convertFRDirectory()` | Signature: `map_paths(input_dir, output_dir, files)`; character input/output roots and file paths under `input_dir` |
| `parse_time_to_frame()` | — | `add_delta_column()`<br>`convert_to_episodes()`<br>`prepare_synchrony_inputs()` | Signature: `parse_time_to_frame(video_time, fps)`; character time values or numeric seconds/frames and numeric frames-per-second |
| `reaction_rate()` | `build_reaction_rate_episode_table()`<br>`prepare_reaction_rate_inputs()` | — | Signature: `reaction_rate(coded_data, time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", exclude_start = 0.1, exclude_start_frames = NULL, fps = 30L, subject_names = NULL, exclude_emotions = "neutral", minimum_threshold = 0)`; converted `fr_coding` object or validated coding table with coding, episode, delta, state, and frame fields; valid window/filter/threshold controls |
| `reaction_rate_by_episode()` | `build_reaction_rate_episode_table()`<br>`prepare_reaction_rate_inputs()` | — | Signature: `reaction_rate_by_episode(coded_data, time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", exclude_start = 0.1, exclude_start_frames = NULL, fps = 30L, subject_names = NULL, exclude_emotions = "neutral", minimum_threshold = 0)`; same input structure and validation controls as `reaction_rate()` |
| `shared_synchronous_episodes()` | `build_synchrony_episode_table()`<br>`prepare_synchrony_inputs()` | — | Signature: `shared_synchronous_episodes(coded_data, subject = "subject", id = "id", time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", fps = 30L, missing_threshold = 0, exclude_emotions = "neutral")`; converted coding object with episode `max_value` and required id/subject/emotion/frame/state/run fields |
| `synchrony()` | `build_synchrony_episode_table()`<br>`prepare_synchrony_inputs()` | — | Signature: `synchrony(coded_data, subject = "subject", id = "id", time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", fps = 30L, missing_threshold = 0, exclude_emotions = "neutral")`; converted coding object with required coding and episode id/subject/emotion/time/value/state/run/frame fields |
| `synchrony_by_episode()` | `build_synchrony_episode_table()`<br>`prepare_synchrony_inputs()` | — | Signature: `synchrony_by_episode(coded_data, subject = "subject", id = "id", time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", fps = 30L, missing_threshold = 0, exclude_emotions = "neutral")`; same input structure and validation controls as `synchrony()`; returns episode-level results |
| `to_seconds()` | — | — | Signature: `to_seconds(x, digits = 3L)`; character vector matching supported time formats; non-negative numeric precision control |

## Internal and helper functions

| Function | Depends on | Depended on by | Required structure and inputs |
|---|---|---|---|
| `all_deltas()` | — | `add_delta_column()`<br>`convert_to_episodes()` | Signature: `all_deltas(value, k, delta)`; generated native wrapper for a numeric value vector, integer window, and numeric threshold |
| `build_reaction_rate_episode_table()` | — | `reaction_rate()`<br>`reaction_rate_by_episode()` | Signature: `build_reaction_rate_episode_table(inputs)`; validated list from `prepare_reaction_rate_inputs()` containing coding, episodes, deltas, limits, thresholds, and result templates |
| `build_synchrony_episode_table()` | — | `shared_synchronous_episodes()`<br>`synchrony()`<br>`synchrony_by_episode()` | Signature: `build_synchrony_episode_table(inputs)`; validated list from `prepare_synchrony_inputs()` containing coding, episodes, limits, thresholds, and a result template |
| `check_duplicate_timecodes()` | — | `convertFRExcelFiles()`<br>`convertFRFiles()` | Signature: `check_duplicate_timecodes(df, duplicate_timecodes_as_error = TRUE)`; imported FaceReader table with `Video Time` and optional participant/index fields |
| `detect_fr_header_row()` | — | `convertFRExcelFiles()`<br>`convertFRFiles()` | Signature: `detect_fr_header_row(lines)`; character lines containing a FaceReader `Video Time` header |
| `hysteresis_state()` | — | `convert_to_episodes()` | Signature: `hysteresis_state(v, k, T_up, T_down, delta, min_len, consecutive_missing)`; generated native wrapper for values and episode threshold/window controls |
| `is_reaction_rate_scalar()` | — | `is_reaction_rate_whole()`<br>`prepare_reaction_rate_inputs()` | Signature: `is_reaction_rate_scalar(x)`; length-one non-missing value |
| `is_reaction_rate_whole()` | `is_reaction_rate_scalar()` | `prepare_reaction_rate_inputs()` | Signature: `is_reaction_rate_whole(x)`; numeric scalar satisfying the package whole-number tolerance |
| `max_delta_ranges()` | — | `convert_to_episodes()` | Signature: `max_delta_ranges(value, start_row, end_row, group_start_row, k)`; generated native wrapper for values, inclusive ranges, group starts, and integer expansion window |
| `prepare_reaction_rate_inputs()` | `is_reaction_rate_scalar()`<br>`is_reaction_rate_whole()` | `reaction_rate()`<br>`reaction_rate_by_episode()` | Signature: `prepare_reaction_rate_inputs(coded_data, time_limit = 3, time_limit_frames = NULL, exclude_start = 0.1, exclude_start_frames = NULL, fps = 30L, subject_names = NULL, exclude_emotions = "neutral", minimum_threshold = 0, constraint_method = "episode")`; converted `fr_coding` object or coding table with required coding/episode/delta fields and validated controls |
| `prepare_shared_synchrony_clips()` | — | `export_shared_synchrony_clips()` | Signature: `prepare_shared_synchrony_clips(coded_data, shared_synchrony, video_paths, n, emotion, buffer = 0, buffer_units = c("seconds", "frames"), buffer_frames = 0L, buffer_seconds = 0)`; validated `fr_coding` metadata, shared interval table with required identifiers/frame/value fields, named existing video paths, and buffer controls |
| `prepare_synchrony_inputs()` | `parse_time_to_frame()` | `shared_synchronous_episodes()`<br>`synchrony()`<br>`synchrony_by_episode()` | Signature: `prepare_synchrony_inputs(coded_data, subject = "subject", id = "id", time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", fps = 30L, missing_threshold = 0, exclude_emotions = "neutral")`; converted coding object with coding and episode tables, selected id/subject columns, and required emotion/time/value/state/run/frame fields |

## Notes

- Export status is authoritative from `NAMESPACE`: 18 exported functions are listed in the first table and 12 internal/helper functions in the second table.
- Both tables use the complete project-function graph. Dependency cells include internal/helper functions as well as exported functions; base R and third-party package calls are excluded.
- `all_deltas()`, `hysteresis_state()`, and `max_delta_ranges()` are generated Rcpp wrappers in `R/RcppExports.R`; their native implementations were not inspected as R functions.
- Functions defined locally inside top-level functions, including validation closures and file-processing closures, are not package-level project functions and are not listed as rows or definite graph dependencies.
- Calls through dynamic dispatch, non-standard evaluation, strings, native `.Call()` implementations, and runtime-generated objects were not treated as definite dependencies. No duplicate top-level function definitions were found.
